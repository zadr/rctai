{
  "name": "@rctai/preview",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "render": "tsc -p tsconfig.json && node dist/cli.js"
  }
}
